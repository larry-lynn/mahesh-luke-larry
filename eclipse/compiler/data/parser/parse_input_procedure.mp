Program parse_input_procedure;

var 
Three, Blind, Mice : integer;
PROCEDURE Initialize( var something, different: integer );
    var local_variable: boolean;
    Begin
        local_variable := 1;
        something := 0;
        different := -1;
    End
    ;

BEGIN
three := (5 + 8);
END
.
